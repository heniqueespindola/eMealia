---
data: 2026-07-24
feature: "Despensa (inventário de ingredientes)"
research: "thoughts/shared/research/2026-07-24-despensa.md"
status: completo
---

# Spec: Despensa (Inventário de Ingredientes)

## Visão Geral
Implementa o ecrã F05 (`apps/mobile/app/(tabs)/pantry.tsx`), hoje um stub, com CRUD completo de `pantry_items` agrupado por categoria, scanner de código de barras via Open Food Facts, alerta de validade próxima, limite de plano Grátis, e integração "cozinhar agora" com o ecrã de pesquisa (F04) via um store Zustand partilhado.

## Decisões confirmadas com o utilizador
1. **Categoria**: nova coluna `categoria` em `pantry_items` (não inferida no cliente), com default `'outros'`.
2. **Partilha despensa → pesquisa**: `pantryStore` (Zustand) passa a ser a fonte única de itens da despensa, consumida tanto por `pantry.tsx` como por `search.tsx`. A navegação "cozinhar agora" usa apenas um param de trigger (`usarDespensa=1`), não serializa a lista.
3. **Autocomplete**: reutiliza `useIngredientAutocomplete` / Edge Function `autocomplete-ingredients` já existente (Spoonacular) tal como está — sem lista estática pt-PT nova.

## Ficheiros a Criar

### `apps/mobile/src/constants/pantry.ts`
**Propósito:** categorias da despensa, alerta de validade e agrupamento/ordenação de itens.
**Conteúdo:**
```ts
import type { CategoriaDespensa, PantryItem } from '@emealia/types';

export const CATEGORIAS_DESPENSA: { value: CategoriaDespensa; label: string; icon: string }[] = [
  { value: 'frescos',    label: 'Frescos',    icon: 'leaf-outline' },
  { value: 'secos',      label: 'Secos',      icon: 'archive-outline' },
  { value: 'congelados', label: 'Congelados', icon: 'snow-outline' },
  { value: 'outros',     label: 'Outros',     icon: 'ellipsis-horizontal-outline' },
];

export const DIAS_ALERTA_VALIDADE = 3;

export function isExpiringSoon(expiraEm: string | null): boolean {
  if (!expiraEm) return false;
  const hoje = new Date();
  hoje.setHours(0, 0, 0, 0);
  const diffDias = Math.ceil((new Date(expiraEm).getTime() - hoje.getTime()) / 86400000);
  return diffDias <= DIAS_ALERTA_VALIDADE;
}

export function agruparPorCategoria(items: PantryItem[]) {
  return CATEGORIAS_DESPENSA.map((cat) => ({
    categoria: cat.value,
    label:     cat.label,
    data:      items
      .filter((i) => i.categoria === cat.value)
      .sort((a, b) => {
        if (a.expira_em && b.expira_em) return a.expira_em.localeCompare(b.expira_em);
        if (a.expira_em) return -1;
        if (b.expira_em) return 1;
        return a.nome.localeCompare(b.nome);
      }),
  })).filter((section) => section.data.length > 0);
}
```

### `apps/mobile/src/lib/openFoodFacts.ts`
**Propósito:** cliente directo (sem Edge Function) para a Open Food Facts API v2.
**Conteúdo:**
```ts
export interface OpenFoodFactsProduct {
  nome:      string;
  categoria: string | null;
}

export async function getProductByBarcode(barcode: string): Promise<OpenFoodFactsProduct | null> {
  const response = await fetch(`https://world.openfoodfacts.org/api/v2/product/${barcode}.json`);
  if (!response.ok) return null;
  const data = await response.json();
  if (data.status !== 1 || !data.product) return null;
  return {
    nome:      data.product.product_name_pt || data.product.product_name || barcode,
    categoria: data.product.categories?.split(',')[0]?.trim() ?? null,
  };
}
```
**Nota:** não há precedente no repo para o shape exacto da resposta OFF v2 — confirmar `product_name_pt`/`product_name`/`categories` com um barcode real durante a implementação e ajustar se necessário.

### `apps/mobile/src/components/ui/Card.tsx`
**Propósito:** container genérico reutilizável (substitui `View` inline repetido em `RecipeCard`/pantry).
**Conteúdo:**
```tsx
import { View, ViewProps } from 'react-native';
import { colors, radius, spacing } from '@/constants/theme';

export function Card({ style, children, ...rest }: ViewProps) {
  return (
    <View {...rest} style={[{ backgroundColor: colors.bgDarkAlt, borderRadius: radius.lg, padding: spacing.md }, style]}>
      {children}
    </View>
  );
}
```

### `apps/mobile/src/components/ui/Badge.tsx`
**Propósito:** badge de destaque (usado no alerta de validade próxima).
**Conteúdo:**
```tsx
import { View, Text } from 'react-native';
import { colors, fonts, radius } from '@/constants/theme';

interface BadgeProps {
  label:   string;
  variant?: 'default' | 'alerta';
}

export function Badge({ label, variant = 'default' }: BadgeProps) {
  const isAlerta = variant === 'alerta';
  return (
    <View style={{
      backgroundColor: isAlerta ? colors.primaryDark : colors.primary,
      borderRadius: radius.full, paddingHorizontal: 8, paddingVertical: 2, alignSelf: 'flex-start',
    }}>
      <Text style={{ fontFamily: fonts.medium, fontSize: 11, color: isAlerta ? colors.textInverted : colors.primaryDark }}>
        {label}
      </Text>
    </View>
  );
}
```

### `apps/mobile/src/components/pantry/BarcodeScanner.tsx`
**Propósito:** scanner de código de barras via `expo-camera` (já instalado, permissões já configuradas em `app.json`).
**Conteúdo:**
- `useCameraPermissions()` — se `!permission.granted`, mostra ecrã de pedido de permissão com `Button` "Permitir câmara" + link "Cancelar" (`onClose`).
- `CameraView` com `barcodeScannerSettings={{ barcodeTypes: ['ean13', 'ean8', 'upc_a'] }}` e `onBarcodeScanned`.
- Guard `scanned` local para não disparar `onScanned` múltiplas vezes no mesmo frame.
- Props: `{ onScanned: (barcode: string) => void; onClose: () => void }`.

### `apps/mobile/src/components/pantry/PantryItemCard.tsx`
**Propósito:** card de item da despensa com swipe-to-delete, toque para editar, toque longo para eliminar (alternativa acessível ao swipe), badge de validade.
**Conteúdo:**
- `Swipeable` (de `react-native-gesture-handler`, já instalado) com `renderRightActions` mostrando botão de eliminar (`Ionicons name="trash-outline"`, fundo `colors.primaryDark`).
- `Pressable` (`onPress={onEdit}`, `onLongPress={confirmDelete}`) envolvendo um `Card` com nome, quantidade (se existir) e `Badge label="Expira em breve" variant="alerta"` quando `isExpiringSoon(item.expira_em)`.
- `confirmDelete` usa `Alert.alert` (título "Eliminar item") antes de chamar `onDelete`.
- Props: `{ item: PantryItem; onEdit: () => void; onDelete: () => void }`.

### `apps/mobile/src/components/pantry/PantryItemForm.tsx`
**Propósito:** modal de criar/editar item — texto com autocomplete, quantidade, validade, categoria, scanner de barcode.
**Conteúdo:**
- `Modal` (`animationType="slide"`) com `ScrollView` (`bgDark`).
- Campos: `Input` "Nome" (+ `IngredientAutocompleteList` alimentada por `useIngredientAutocomplete(nome)`), `Input` "Quantidade", `Input` "Validade (AAAA-MM-DD)" (texto livre validado por regex `/^\d{4}-\d{2}-\d{2}$/` antes de submeter — sem nova dependência de date-picker, ver Notas de Implementação), `Pill` por categoria (`CATEGORIAS_DESPENSA`).
- Botão "Ler código de barras" (oculto em modo edição) que abre `BarcodeScanner` num segundo `Modal`; `onScanned` chama `getProductByBarcode`, pré-preenche `nome` se encontrado, ou mostra aviso "Produto não encontrado. Podes continuar a adicionar manualmente." (`naoEncontrado` state) mantendo o formulário aberto para preenchimento manual.
- `useEffect` a repor o estado do formulário sempre que `visible`/`initialValues` mudam (suporta reabrir em modo criar após editar).
- Botão de submissão desativado se `!nome.trim()` ou (`limitReached && !isEditing`); mostra aviso de limite + sugestão de upgrade quando `limitReached` em modo criação.
- Props: `{ visible: boolean; onClose: () => void; onSubmit: (values: { nome: string; quantidade: string | null; expira_em: string | null; categoria: CategoriaDespensa; barcode: string | null }) => Promise<void>; initialValues?: Partial<PantryItem>; limitReached: boolean }`.

## Ficheiros a Modificar

### `supabase/schema.sql`
- [x] Após o bloco `─── Pantry Items` (depois de `CREATE INDEX IF NOT EXISTS pantry_items_user_id_idx`), adicionar de forma idempotente:
```sql
ALTER TABLE pantry_items
  ADD COLUMN IF NOT EXISTS categoria text NOT NULL DEFAULT 'outros'
  CHECK (categoria IN ('frescos','secos','congelados','outros'));
```

### `packages/types/src/pantry.ts`
- [x] Adicionar `export type CategoriaDespensa = 'frescos' | 'secos' | 'congelados' | 'outros';`
- [x] Adicionar `categoria: CategoriaDespensa;` a `PantryItem` (entre `barcode` e `expira_em`, ou junto a `expira_em` — manter ordem legível).

### `packages/types/src/database.ts`
- [x] Importar `CategoriaDespensa` de `./pantry`.
- [x] No bloco `pantry_items`, tornar `categoria` opcional apenas no `Insert` (tem default na BD):
```ts
pantry_items: {
  Row:           Simplify<PantryItem>;
  Insert:        Simplify<Omit<PantryItem, 'id' | 'created_at' | 'categoria'> & { categoria?: CategoriaDespensa }>;
  Update:        Simplify<Partial<PantryItem>>;
  Relationships: [];
};
```

### `packages/supabase/src/queries/pantry.ts`
- [x] Trocar as assinaturas de `addPantryItem`/`addPantryItems` de `Omit<PantryItem, 'id' | 'created_at'>` para `Database['public']['Tables']['pantry_items']['Insert']` (agora que `categoria` é opcional no Insert, chamadas existentes como `addPantryItems` no onboarding continuam válidas sem alterações).
- [x] Adicionar:
```ts
export async function updatePantryItem(
  client: SupabaseClient<Database>,
  id: string,
  updates: Partial<PantryItem>
) {
  return client.from('pantry_items').update(updates).eq('id', id).select().single();
}
```

### `apps/mobile/src/stores/pantryStore.ts`
- [x] Reescrever para seguir o padrão de `loadedUserId` (mesmo mecanismo de `useProfile`/`profileStore`, adaptado a lista) e adicionar `updateItem`:
```ts
import { create } from 'zustand';
import type { PantryItem } from '@emealia/types';

interface PantryState {
  items:        PantryItem[];
  loading:      boolean;
  loadedUserId: string | null;
  setItems:     (userId: string, items: PantryItem[]) => void;
  setLoading:   (loading: boolean) => void;
  addItem:      (item: PantryItem) => void;
  updateItem:   (item: PantryItem) => void;
  removeItem:   (id: string) => void;
  reset:        () => void;
}

export const usePantryStore = create<PantryState>((set) => ({
  items:        [],
  loading:      true,
  loadedUserId: null,
  setItems:     (userId, items) => set({ items, loadedUserId: userId, loading: false }),
  setLoading:   (loading) => set({ loading }),
  addItem:      (item)  => set((s) => ({ items: [item, ...s.items] })),
  updateItem:   (item)  => set((s) => ({ items: s.items.map((i) => (i.id === item.id ? item : i)) })),
  removeItem:   (id)    => set((s) => ({ items: s.items.filter((i) => i.id !== id) })),
  reset:        ()      => set({ items: [], loadedUserId: null, loading: false }),
}));
```

### `apps/mobile/src/hooks/usePantry.ts`
- [x] Reescrever por completo seguindo o padrão de `useProfile.ts` (store partilhado + queries de `@emealia/supabase`, nunca lança, `console.error` + fallback):
```ts
import { useEffect } from 'react';
import { supabase } from '@/lib/supabase';
import { getPantry, addPantryItem, updatePantryItem, deletePantryItem } from '@emealia/supabase';
import { usePantryStore } from '@/stores/pantryStore';
import type { PantryItem, Database } from '@emealia/types';

type PantryInsert = Database['public']['Tables']['pantry_items']['Insert'];

export function usePantry(userId: string | undefined) {
  const items   = usePantryStore((s) => s.items);
  const loading = usePantryStore((s) => s.loading);

  useEffect(() => {
    if (!userId) {
      usePantryStore.getState().reset();
      return;
    }
    if (usePantryStore.getState().loadedUserId === userId) return;
    fetchItems(userId);
  }, [userId]);

  async function fetchItems(uid: string) {
    usePantryStore.getState().setLoading(true);
    const { data, error } = await getPantry(supabase!, uid);
    if (error) console.error('[usePantry] getPantry falhou:', error);
    usePantryStore.getState().setItems(uid, data ?? []);
  }

  async function add(item: Omit<PantryInsert, 'user_id'>) {
    if (!userId) return;
    const { data, error } = await addPantryItem(supabase!, { ...item, user_id: userId });
    if (error) { console.error('[usePantry] addPantryItem falhou:', error); return; }
    if (data) usePantryStore.getState().addItem(data);
  }

  async function update(id: string, updates: Partial<PantryItem>) {
    const { data, error } = await updatePantryItem(supabase!, id, updates);
    if (error) { console.error('[usePantry] updatePantryItem falhou:', error); return; }
    if (data) usePantryStore.getState().updateItem(data);
  }

  async function remove(id: string) {
    const { error } = await deletePantryItem(supabase!, id);
    if (error) { console.error('[usePantry] deletePantryItem falhou:', error); return; }
    usePantryStore.getState().removeItem(id);
  }

  function refetch() {
    if (userId) fetchItems(userId);
  }

  return { items, loading, add, update, remove, refetch };
}
```
**Nota:** `refetch` ignora `loadedUserId` deliberadamente — é o mecanismo usado pelo pull-to-refresh.

### `apps/mobile/app/(tabs)/pantry.tsx`
- [x] Substituir o stub completo por:
  - `useAuth` + `useProfile(user?.id)` (para `profile.plano`) + `usePantry(user?.id)`.
  - `limit = profile?.plano === 'free' ? LIMITS.free.pantry_items : LIMITS.premium.pantry_items` (de `@emealia/config`); `limitReached = items.length >= limit`.
  - `SectionList` com `sections={agruparPorCategoria(items)}`, `renderSectionHeader` (label da categoria), `renderItem` → `PantryItemCard`, `refreshControl` ligado a `refetch` (via estado local `refreshing`), `ListEmptyComponent` com mensagem "A tua despensa está vazia...".
  - Banner (`Card`) visível quando `limitReached`, com mensagem de limite atingido + sugestão de upgrade.
  - Rodapé com dois `Button`: "+ Adicionar" (`disabled={limitReached}`, abre `PantryItemForm` em modo criar) e "Cozinhar agora" (`disabled={items.length === 0}`, `router.push({ pathname: '/(tabs)/search', params: { usarDespensa: '1' } })`).
  - `PantryItemForm` controlado por `formVisible`/`editingItem`; `onSubmit` chama `update(editingItem.id, values)` ou `add(values)` consoante o modo, depois fecha o modal.
  - Fundo `colors.bgDark` (alinhado com `index.tsx`/`search.tsx` — não usar `bg-bgLight` do stub actual).

### `apps/mobile/app/(tabs)/search.tsx`
- [x] Trocar `usePantry(user?.id ?? '')` por `usePantry(user?.id)` (assinatura agora aceita `undefined`).
- [x] Importar `useLocalSearchParams` de `expo-router` e ler `usarDespensa` param.
- [x] Adicionar `useEffect` que, quando `usarDespensa === '1'` e `!usandoDespensa` e `pantryItems.length > 0`, chama `setUsandoDespensa(true)` + `usarDespensa(pantryItems)` — replica exactamente o comportamento do toggle manual "Usar despensa" (mesmo mecanismo one-way/one-shot já existente, sem alterar `useRecipeSearch.ts`).

### `apps/mobile/app/_layout.tsx`
- [x] Importar `GestureHandlerRootView` de `react-native-gesture-handler`.
- [x] Envolver o `<Stack .../>` retornado com `<GestureHandlerRootView style={{ flex: 1 }}>...</GestureHandlerRootView>` — necessário para o `Swipeable` do `PantryItemCard` funcionar correctamente no Android.

## Fases de Implementação

### Fase 1: Schema, tipos e queries partilhadas
**Ficheiros:**
- Modificar `supabase/schema.sql`, `packages/types/src/pantry.ts`, `packages/types/src/database.ts`, `packages/supabase/src/queries/pantry.ts`

**Critérios de sucesso (automáticos):**
- [x] `npm run typecheck` passa sem erros (incluindo `apps/mobile/app/onboarding/step3.tsx`, que continua a chamar `addPantryItems` sem `categoria`)
- [ ] Script `schema.sql` corre sem erro no Supabase SQL Editor (correr duas vezes seguidas para confirmar idempotência)

**Critérios de sucesso (manuais):**
- [ ] Uma linha existente em `pantry_items` (se houver dados de teste) passa a ter `categoria = 'outros'` após correr o `ALTER TABLE`

### Fase 2: Estado partilhado (store + hook)
**Ficheiros:**
- Modificar `apps/mobile/src/stores/pantryStore.ts`, `apps/mobile/src/hooks/usePantry.ts`

**Critérios de sucesso (automáticos):**
- [x] `tsc --noEmit` passa sem erros

**Critérios de sucesso (manuais):**
- [ ] `console.error` aparece (sem crash) se `getPantry` falhar com um `userId` inválido

### Fase 3: Componentes base e utilitários
**Ficheiros:**
- Criar `apps/mobile/src/constants/pantry.ts`, `apps/mobile/src/components/ui/Card.tsx`, `apps/mobile/src/components/ui/Badge.tsx`

**Critérios de sucesso (automáticos):**
- [x] `tsc --noEmit` passa sem erros
- [x] `expo lint` sem warnings

### Fase 4: Scanner de código de barras
**Ficheiros:**
- Criar `apps/mobile/src/lib/openFoodFacts.ts`, `apps/mobile/src/components/pantry/BarcodeScanner.tsx`

**Critérios de sucesso (manuais):**
- [ ] No simulador/dispositivo, negar a permissão de câmara mostra o ecrã de pedido de permissão (não crasha)
- [ ] Ler um código de barras real (ex: produto de supermercado português) devolve nome via Open Food Facts
- [ ] Ler um código de barras inexistente na OFF não crasha e permite continuar

### Fase 5: Ecrã da despensa
**Ficheiros:**
- Criar `apps/mobile/src/components/pantry/PantryItemCard.tsx`, `apps/mobile/src/components/pantry/PantryItemForm.tsx`
- Modificar `apps/mobile/app/(tabs)/pantry.tsx`, `apps/mobile/app/_layout.tsx`

**Critérios de sucesso (automáticos):**
- [x] `tsc --noEmit` passa sem erros
- [x] `expo lint` sem warnings

**Critérios de sucesso (manuais):**
- [ ] Adicionar item por texto (com autocomplete) aparece na secção de categoria correcta
- [ ] Editar quantidade/validade de um item persiste após pull-to-refresh
- [ ] Swipe para a esquerda + toque longo eliminam o item (com confirmação)
- [ ] Item com `expira_em` a 3 dias ou menos mostra badge de alerta; item já expirado também
- [ ] Com 20 itens no plano `free`, "+ Adicionar" fica desactivado e mostra aviso de upgrade; num perfil `premium_monthly`/`premium_annual` não há limite
- [ ] Pull-to-refresh recarrega a lista a partir do Supabase

### Fase 6: Integração "cozinhar agora"
**Ficheiros:**
- Modificar `apps/mobile/app/(tabs)/search.tsx`

**Critérios de sucesso (manuais):**
- [ ] A partir da despensa com pelo menos 1 item, tocar em "Cozinhar agora" abre a pesquisa com os ingredientes da despensa já como chips seleccionados e o Pill "Usar despensa" activo
- [ ] O toggle manual "Usar despensa" em `search.tsx` continua a funcionar exactamente como antes (regressão)

## Estratégia de Testes
- **Automático:** `npm run typecheck` na raiz do monorepo após cada fase; `expo lint` em `apps/mobile`.
- **Manual:** correr `npm run mobile`, testar no simulador iOS (permissão de câmara negada/concedida) e idealmente num dispositivo Android físico (scanner de barcode costuma comportar-se de forma diferente em simulador). Testar os fluxos descritos nos critérios manuais de cada fase, incluindo o caso de limite de plano com um perfil de teste `plano = 'free'` com 20 itens.

## Notas de Implementação
- **Limite de 20 itens é 100% client-side** (sem enforcement em RLS/Edge Function) — risco aceite para o MVP, consistente com o resto do repo (nenhuma outra feature tem enforcement server-side de limites de plano).
- **Validade (`expira_em`) editada por texto livre** (`AAAA-MM-DD` validado por regex), não por date-picker nativo — evita introduzir uma nova dependência (`@react-native-community/datetimepicker`) sem precedente no repo; mantém-se o mesmo padrão já usado para `quantidade` (texto livre). Se a UX se revelar fraca em testes manuais, considerar adicionar o date-picker como iteração seguinte.
- **`react-native-gesture-handler`/`expo-camera` já estão instalados** — não são necessárias novas dependências nem alterações a `app.json` (permissões de câmara já configuradas em `NSCameraUsageDescription`/`CAMERA`/plugin `expo-camera`).
- **Open Food Facts**: chamada directa do cliente (sem `SPOONACULAR_API_KEY`/Edge Function), conforme já assumido no ticket — confirmar o shape exacto da resposta (`product_name_pt`, `product_name`, `categories`) com um barcode real durante a implementação.
- **`usarDespensa` continua one-way/one-shot** — desligar o toggle em `search.tsx` não remove os ingredientes já injectados (comportamento herdado de F04, não alterado aqui).
- **`Card`/`Badge`** ficam em `src/components/ui/` para reutilização futura (ex: F07 lista de compras), mas o escopo desta feature é só usá-los na despensa.

## Referências
- Research: `thoughts/shared/research/2026-07-24-despensa.md`
- Ticket: `thoughts/shared/tickets/2026-07-24-despensa.md`
- Padrão de hook CRUD: `apps/mobile/src/hooks/useProfile.ts`
- Padrão de `.update()`: `packages/supabase/src/queries/profile.ts`
- Mecanismo "usar despensa" (a reaproveitar): `apps/mobile/src/hooks/useRecipeSearch.ts:48-58`, `apps/mobile/app/(tabs)/search.tsx:48-54`
